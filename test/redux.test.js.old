//https://redux.js.org/recipes/writing-tests

NON FUNZIONA IL FRAMEWORK CON CUI FARLI GIRARE 

import authReducers from '../client/src/reducers/auth';
import * as ActionTypes from '../client/src/actions/types';

describe('Auth reducers', () => {
  it('inital state', () => {
    expect(
      authReducers(undefined, {}).toEqual([
        { token: null, isAuthenticated: null, loading: true, user: null },
      ])
    );
  });
});


